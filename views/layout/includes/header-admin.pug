header.bg-indigo-600.p-4
    div.container.mx-auto.flex.justify-between.items-center
        a(href="/")
            h1.text-2xl.text-white.font-extrabold.text-center REAL
                span.font-normal ESTATE
        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white")
            a(href="/my-properties?page=1") My properties
            a(href=`/auth/user-profile`) Profile
            form(action="/auth/logout" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken)
                input(type="submit" class="bg-indigo-800 py-2 px-10 rounded-lg hover:bg-indigo-900 text-center" value="Log out")